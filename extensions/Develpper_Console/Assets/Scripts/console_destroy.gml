///console_destroy();
//This script destroy all existing consoles. Even if only one should be active at the time.
instance_destroy(object_main_console);
instance_destroy(object_instance_follower);
